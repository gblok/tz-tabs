export const duty = ({tx, doc}) => doc

export const schemes = ({tx, doc}) => {
   // console.log({doc})
    return doc
}
export const pages = ({tx, doc}) => doc
export const products = ({tx, doc}) => doc